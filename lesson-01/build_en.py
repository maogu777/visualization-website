import json

src = r'C:\Users\HY\WorkBuddy\2026-09-06-16-08-39\docs\portal\lesson-01\zh_blocks.json'
dst = r'C:\Users\HY\WorkBuddy\2026-09-06-16-08-39\docs\portal\lesson-01\en_blocks.json'

with open(src, encoding='utf-8') as f:
    blocks = json.load(f)

trans = {
0: "Explains asynchronous loading and field mapping using the new-student dataset (synthesized by DeepSeek)",
1: "Your Original Case",
2: "This chapter uses the 2025 new-student data from China University of Petroleum (East China) to illustrate the characteristics of data. To prevent data privacy leaks, the data was generated by the <strong>DeepSeek large language model</strong>. The data file <code>data/newstudent.csv</code> contains 80 records.",
3: "Embedded Theory: The Trustworthiness Boundary of AI-Synthesized Data",
4: "<strong>Four Dimensions of Data Quality</strong> (Wang &amp; Strong 1996): Accuracy / Completeness / Consistency / Timeliness.",
5: "Data Generated by DeepSeek:",
6: "✅ Completeness: all fields present",
7: "⚠️ Accuracy: names/hometowns are fictional and cannot be used for real demographic analysis",
8: "⚠️ Sampling bias: the model tends toward 'plausible-looking' distributions (e.g., over-representing the Han ethnicity, favoring large cities for hometowns)",
9: "<strong>Classroom Takeaway</strong>: Using synthetic data in teaching is fine, but it must be labeled as 'synthetic data' to prevent students from mistaking it for a real distribution.",
10: "Key Code ① Field Name Mapping",
11: "Map Chinese column names to English variable names — to avoid syntax/encoding issues caused by Chinese keys in JS.",
12: "\n              <strong>Key-Point Explanation:</strong> Why not use Chinese keys?\n              (1) JS engines have poor tolerance for Chinese identifiers (older browsers throw errors); (2) backends, JSON, and URL encoding may introduce mojibake;\n              (3) IDE autocomplete, search, and refactoring tools have inconsistent support for Chinese.\n              <strong>Best Practice</strong>: Use human-readable Chinese names in CSV, and use unified English variables internally in JS.\n            ",
13: "Key Code ② Type Conversion Functions",
14: "After CSV loading, all fields are strings; you need to convert the index/age to Number and the birthday to a Date object.",
15: "\n              <strong>Key-Point Explanation:</strong>\n              <code>+d[\"年龄\"]</code> is the unary plus operator, the fastest way in JS to convert a string to a number (shorter than <code>Number()</code> / <code>parseInt()</code>);\n              <code>new Date(str)</code> can parse ISO formats like <code>\"2007-08-15\"</code>, but has poor compatibility with <code>\"2007/8/15\"</code>, so fall back to <code>d3.timeParse(\"%Y-%m-%d\")</code>.\n              <strong>Pitfall</strong>: Without <code>trim()</code>, the trailing space in \"软件工程1班 \" will cause later filtering/aggregation to fail.\n            ",
16: "Key Code ③ Pass the row callback directly as the second argument of d3.csv",
17: "\n              <strong>Key-Point Explanation:</strong> d3.csv is an asynchronous function and must be <code>await</code>ed.\n              The row callback in the second argument is <strong>invoked immediately after each row is parsed</strong> — which saves memory compared to first <code>await d3.csv(...)</code> then <code>.map(transformRow)</code> (transform while reading).\n              <strong>Significance of Asynchronous Loading</strong>: It does not block page rendering, so the user can keep interacting.\n            ",
18: "Click 'Reload' to see the asynchronous CSV loading, field mapping, and type conversion → output the transformed table + console logs.",
19: "DIKW Pyramid — Data → Information → Knowledge → Wisdom",
20: "Your Original Case",
21: "DIKW Pyramid: Data is 'raw data', Information is 'organized data', Knowledge is 'patterns/regularities', and Wisdom is 'judgment/strategy'.",
22: "\n              <thead><tr><th>Tier</th><th>Term</th><th>Question Answered</th></tr></thead>\n              <tbody>\n                <tr><td>Data</td><td>Raw, discrete observations</td><td>—</td></tr>\n                <tr><td>Information</td><td>Data that has been processed, organized, and given context</td><td>Who / What / When / Where</td></tr>\n                <tr><td>Knowledge</td><td>Further refinement and understanding of information</td><td>How</td></tr>\n                <tr><td>Wisdom</td><td>Applying knowledge to make wise judgments and strategic decisions</td><td>Why / What to do</td></tr>\n              </tbody>\n            ",
23: "Embedded Theory: The Position of Visualization in the DIKW Model",
24: "<strong>Key Insight</strong>: Visualization mainly lifts <strong>Data to Information</strong>; in a few scenarios it can reach <strong>Knowledge</strong>, but it is difficult to cross over to Wisdom.",
25: "✅ Data → Information: 'Translate' the table into readable graphics (line chart, bar chart, heatmap)",
26: "⚠️ Information → Knowledge: Pattern discovery requires statistical models (machine learning)",
27: "❌ Knowledge → Wisdom: Decisions and strategy are beyond the capability of visualization",
28: "<strong>Rowley (2007) Critique</strong>: DIKW is an oversimplified hierarchical model; in reality the boundary between Data and Information is blurred. But as a teaching scaffold, it remains most valuable.",
29: "Key Code ① SVG Pyramid Path",
30: "The pyramid is composed of 4 trapezoids joined together — the d attribute of the D3 path.",
31: "\n              <strong>Key-Point Explanation</strong>: The SVG path commands <code>M</code> (moveto), <code>L</code> (lineto), and <code>Z</code> (close) are the three most commonly used.\n              Template literals <code>`${...}`</code> can embed JS variables directly, making them more readable than string concatenation.\n              <strong>Practice</strong>: For complex shapes, use D3's <code>d3.symbol()</code> (which has built-in ★●▲ etc.); hand-writing paths is error-prone.\n            ",
32: "Key Code ② Interactive Highlighting (hover/click)",
33: "\n              <strong>Key-Point Explanation</strong>: <strong>You must use <code>function()</code> instead of arrow functions</strong> —\n              because D3 callbacks use <code>this</code> to access the current element, and arrow functions capture the outer <code>this</code>, causing it to fail. This is the most common D3 pitfall.\n              <code>transition().duration()</code> is D3's built-in animation API, even more concise than jQuery.\n            ",
34: "Hover over the pyramid → highlight that tier · click → show that tier's definition / examples / common visualizations on the right",
35: "Stevens Measurement Scales × Mackinlay Visual Channel Ranking",
36: "Your Original Case (3 Categories)",
37: "Your original text gives 3 types of attributes:",
38: "<strong>Categorical Attribute</strong>: a symbol or name with no ordering (gender, name)",
39: "<strong>Ordinal Attribute</strong>: attribute values have an order (degree: bachelor's/master's/doctorate)",
40: "<strong>Numerical Attribute</strong>: quantitative method (age, price)",
41: "Embedded Theory: Stevens Measurement Scales (1946) — 4 Types",
42: "Stevens divides measurement scales into <strong>4 types</strong>, finer than your original text:",
43: "\n              <thead><tr><th>Category</th><th>Allowed Operations</th><th>Typical Example</th><th>Maps to Your Original</th></tr></thead>\n              <tbody>\n                <tr><td>Nominal</td><td>Equivalence / Non-equivalence</td><td>Name, Gender</td><td>Categorical</td></tr>\n                <tr><td>Ordinal</td><td>+ &gt; &lt;</td><td>Rank, Class index</td><td>Ordinal</td></tr>\n                <tr><td>Interval</td><td>+ Add/Subtract</td><td>Temperature (°C)</td><td>—</td></tr>\n                <tr><td>Ratio</td><td>+ Add/Subtract/Multiply/Divide</td><td>Age, Price</td><td>Numerical</td></tr>\n              </tbody>\n            ",
44: "<strong>Key Difference</strong>: The interval scale has no 'true zero point' (0°C is not 'no temperature'); the ratio scale has a true zero (0 yuan = no money).",
45: "Embedded Theory: Mackinlay Visual Channel Ranking (1986)",
46: "Mackinlay proposes dividing visual channels into 3 tiers:",
47: "\n              <thead><tr><th>Tier</th><th>Channel</th><th>Suitable Attribute</th></tr></thead>\n              <tbody>\n                <tr><td>Quantitative</td><td>Position, Length, Angle</td><td>Ratio / Interval</td></tr>\n                <tr><td>Ordered</td><td>Brightness, Density, Saturation</td><td>Ordinal</td></tr>\n                <tr><td>Qualitative</td><td>Hue, Shape</td><td>Nominal</td></tr>\n              </tbody>\n            ",
48: "<strong>Core Finding</strong>: Position is the strongest quantitative channel; hue is the strongest qualitative channel. Mismatch → information loss.",
49: "Key Code ① Scale Selection",
50: "\n              <strong>Key-Point Explanation</strong>: A D3 scale's <code>domain()</code> is the data domain (input) and <code>range()</code> is the value range (output pixels).\n              Choosing the wrong scale is the most common beginner mistake: <strong>using scaleLinear for categorical data produces meaningless intermediate values</strong> (e.g., class 0.5).\n              <strong>Memory Aid</strong>: Use Linear for continuous quantitative, Band for discrete categorical, and Point for ordinal.\n            ",
51: "Key Code ② Colormap Selection",
52: "\n              <strong>Key-Point Explanation</strong>: Viridis is the benchmark sequential colormap — color-vision-deficiency (CVD) friendly and perceptually uniform.\n              <strong>Common Mistake</strong>: Using the rainbow colormap <code>interpolateRainbow</code> to encode numerical data — it is neither CVD-friendly nor free of false boundaries (blue→green jumps).\n              <strong>Class Question</strong>: When to use a diverging colormap? Answer: Data that has a 'middle value' (e.g., positive/negative, temperature vs. average).\n            ",
53: "4 sub-demos: ① Categorical uses hue (✓) ② Ordinal uses brightness (✓) ③ Numerical uses position (✓) ④ ❌ Numerical uses hue (mismatch — cannot tell magnitude)",
54: "D3's 8 Functional Array Operations",
55: "Your Original Case (8 Functions)",
56: "Your original text lists 8 common functions provided by D3:",
57: "<code>map(iterable, mapper)</code> — similar to Array.map",
58: "<code>reduce(iterable, reducer, init)</code> — similar to Array.reduce",
59: "<code>cross(...iterables, reducer)</code> — Cartesian product",
60: "<code>merge(iterables)</code> — merge into a flat array",
61: "<code>pairs(iterable, reducer)</code> — adjacent element pairs",
62: "<code>transpose(matrix)</code> — matrix transpose",
63: "<code>zip(...arrays)</code> — combine by position",
64: "<code>filter(iterable, test)</code> — similar to Array.filter",
65: "Embedded Theory: Functional Data Flow",
66: "Common characteristics of these 8 functions:",
67: "✅ <strong>Immutable</strong>: input unchanged, returns a new array",
68: "✅ <strong>Pure function</strong>: same input → same output, no side effects",
69: "✅ <strong>Chainable</strong>: returns an array, can be followed by the next transformation",
70: "<strong>Why Functional?</strong>",
71: "Can be reasoned about: each transformation layer is independent and testable",
72: "Parallelizable: map/reduce/filter are naturally parallelizable",
73: "Cacheable: outputs of pure functions can be memoized",
74: "<strong>The Prototype of the Data Cube (OLAP)</strong>: <code>transpose</code> performs wide/long format conversion, and <code>group + rollup</code> performs slicing/dicing/drilling — this is exactly the core of BI systems.",
75: "Key Code ① Chained Composition of map / reduce",
76: "\n              <strong>Key-Point Explanation</strong>: Each step of the chained call returns a new array and does not modify the original array.\n              <strong>Compared with Imperative</strong>: The imperative style needs 4 temporary variables + a for loop, while the functional style does it in one line.\n              <strong>Performance Trade-off</strong>: Each map/filter creates a new array, which incurs a performance cost for very large arrays (only significant above the million level).\n            ",
77: "Key Code ② cross (Cartesian Product)",
78: "\n              <strong>Key-Point Explanation</strong>: The Cartesian product is commonly used in 'pairwise combination' analysis (e.g., counting students by gender × major).\n              The second reducer argument is optional and is used to 'combine' the two elements.\n              <strong>Extension</strong>: The Cartesian product of 3 arrays yields 8 combinations; for n arrays it is the product of their sizes.\n            ",
79: "Key Code ③ group / rollup (Aggregation)",
80: "\n              <strong>Key-Point Explanation</strong>: <code>group</code> vs <code>rollup</code> is the difference between 'keep groups' and 'aggregate groups'.\n              The second argument of <code>rollup</code> is a reducer function (here it takes the length); in later chapter doc-02, rollup will be used in detail for tree-structured data.\n            ",
81: "Use the dropdown to switch among the 8 functions; the right side shows input/output comparison + a real scenario (new-student data)",
82: "Selection and the enter / update / exit Three-State Model",
83: "Your Original Case (6 Selector Methods)",
84: "Your original text outlines 6 selector methods:",
85: "\n              <thead><tr><th>Method</th><th>Returns</th></tr></thead>\n              <tbody>\n                <tr><td><code>d3.select</code></td><td>single element</td></tr>\n                <tr><td><code>d3.selectAll</code></td><td>selection of multiple elements</td></tr>\n                <tr><td><code>selection.select</code></td><td>the first descendant of each selected element</td></tr>\n                <tr><td><code>selection.selectAll</code></td><td>all descendants</td></tr>\n                <tr><td><code>selection.selectChild</code></td><td>the first direct child element</td></tr>\n                <tr><td><code>selection.selectChildren</code></td><td>all direct child elements</td></tr>\n              </tbody>\n            ",
86: "Embedded Theory: The Data-Join Three-State Model (Mike Bostock)",
87: "The core of D3 is <strong>Data-Join</strong>: 'binding' a data array to DOM elements.",
88: "When the data changes:",
89: "New data &gt; old data: the extras enter <strong style=\"color:#10b981\">enter</strong> (need to append)",
90: "New data = old data: the existing elements enter <strong style=\"color:#0ea5e9\">update</strong> (need attribute changes)",
91: "New data &lt; old data: the surplus elements enter <strong style=\"color:#ef4444\">exit</strong> (need to remove)",
92: "Data-Join vs Imperative DOM",
93: "\n              <thead><tr><th>Imperative</th><th>Declarative (Data-Join)</th></tr></thead>\n              <tbody>\n                <tr><td><code>createElement</code> then append one by one</td><td><code>.data(arr).join(...)</code> in one line</td></tr>\n                <tr><td>full re-render</td><td>incremental update (better performance)</td></tr>\n                <tr><td>element identity lost</td><td>key function preserves stable identity (natural animation)</td></tr>\n                <tr><td>disconnected from the data flow</td><td>naturally fits the functional data flow</td></tr>\n              </tbody>\n            ",
94: "Key Code ① .join(enter, update, exit) Three States",
95: "\n              <strong>Key-Point Explanation</strong>: The three callbacks of join are invoked in the enter / update / exit phases respectively.\n              This 'declarative three-state' pattern is more concise than the v3 <code>enter().append()</code> + <code>exit().remove()</code> style, and less error-prone.\n              <strong>Analogy</strong>: React's diff + patch — the same idea, but React does it for you, while D3 lets you write the transformation functions yourself.\n            ",
96: "Key Code ② key Function Preserves Identity",
97: "\n              <strong>Key-Point Explanation</strong>: <strong>The key function is the soul of D3 animation</strong>.\n              Without a key, D3 binds identity by index: reordering data causes 'all elements to move'.\n              With a key, each circle knows its id, and on reorder only cx changes, so the transition animation is natural.\n              <strong>Analogy</strong>: Exactly the same idea as React's <code>key={item.id}</code>.\n            ",
98: "Key Code ③ d3.select vs d3.selectAll",
99: "\n              <strong>Key-Point Explanation</strong>: <code>select</code> is used to 'operate on a single element', and <code>selectAll</code> is used to 'bind data in bulk'.\n              <strong>Common Mistake</strong>: When using <code>selectAll(\".bar\")</code> to bind data to an array, the number of elements must match the number of data items, otherwise it will 'leave gaps' or 'overflow'.\n              <strong>Best Practice</strong>: First use <code>selectAll</code> to create placeholder elements (possibly empty), then <code>.data().join(...)</code>.\n            ",
100: "Click the button to trigger enter (green) / update (blue) / exit (red), and observe the three-state switching",
101: "Doc-01 Integrated Teaching Page · Zero Dependencies · D3.js v7 + Vanilla JS · Standalone",
102: "<a href=\"../\" data-i18n=\"nav.home\" data-i18n=\"common.enter\" data-i18n=\"common.enter\">Course Portal</a>",
103: "<a href=\"../lesson-01/\" data-i18n=\"nav.ch1\">Chapter 1 · Data</a>",
104: "<a href=\"../lesson-02/\" data-i18n=\"nav.ch2\">Chapter 2 · Data Visualization Process</a>",
105: "<a href=\"../lesson-03/\" data-i18n=\"nav.ch3\">Chapter 3 · Color Special Topic</a>",
106: "<a href=\"../lesson-04/\" data-i18n=\"nav.ch4\">Chapter 4 · Low-Dimensional Visualization</a>",
107: "<a href=\"../lesson-05/\" data-i18n=\"nav.ch5\">Chapter 5 · Time-Series Visualization</a>",
}

for b in blocks:
    i = b['idx']
    if i not in trans:
        raise KeyError(f"missing translation for idx {i}")
    b['en'] = trans[i]

with open(dst, 'w', encoding='utf-8') as f:
    json.dump(blocks, f, ensure_ascii=False, indent=2)

print("done", len(blocks))
